import { useOutletContext } from "react-router-dom";import { EmptyState,PageHeader,Timeline,TimelineItem } from "@/shared/ui";import { formatDateTime } from "@/shared/utils/formatters";
export default function ObraTimelinePage(){const {obra,timeline}=useOutletContext();const rows=Array.isArray(timeline)?timeline:[];return <main><PageHeader eyebrow="Obra · Historial" title={`Timeline de ${obra.nombre}`} description="Eventos reales recibidos para esta obra, ordenados por el backend."/>{rows.length?<Timeline>{[...rows].reverse().map((event,index)=><TimelineItem key={`${event.tipo}-${event.referencia_id}-${index}`} timestamp={formatDateTime(event.fecha)} title={event.titulo||String(event.tipo||"Evento").replaceAll("_"," ")} description={event.descripcion||String(event.tipo||"").replaceAll("_"," ")}/>)}</Timeline>:<EmptyState title="Sin eventos registrados" description="Una respuesta exitosa sin eventos no se presenta como error."/>}</main>}
