import { Building2, MapPin } from "lucide-react";
import { useOperationalWorkspace } from "../context/OperationalWorkspaceContext";

export default function WorkspaceSelector({ compact = false }) {
  const { activeWorkspace, selectWorkspace, workspaces } = useOperationalWorkspace();
  if (compact) return <select aria-label="Cambiar espacio de trabajo" className="max-w-64 rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm font-bold text-slate-800" value={activeWorkspace?.id || ""} onChange={(event) => selectWorkspace(workspaces.find((item) => String(item.id) === event.target.value))}><option value="">Seleccionar espacio</option>{workspaces.map((item) => <option key={item.id} value={item.id}>{item.area.nombre} · {item.obra?.nombre || item.organizacion.nombre}</option>)}</select>;
  return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-5"><section className="w-full max-w-2xl rounded-3xl border border-slate-200 bg-white p-7 shadow-xl"><p className="text-xs font-black uppercase tracking-[0.16em] text-emerald-700">Carbono Zero</p><h1 className="mt-2 text-3xl font-black text-slate-950">¿Dónde quieres trabajar?</h1><p className="mt-2 text-slate-600">Selecciona el contexto que corresponde a tu trabajo de hoy.</p><div className="mt-6 grid gap-3">{workspaces.map((item) => <button key={item.id} type="button" onClick={() => selectWorkspace(item)} className="flex items-center gap-4 rounded-2xl border border-slate-200 p-5 text-left transition hover:border-emerald-500 hover:bg-emerald-50"><span className="flex h-11 w-11 items-center justify-center rounded-xl bg-emerald-100 text-emerald-800"><Building2 size={22} /></span><span className="min-w-0"><b className="block text-lg text-slate-950">{item.area.nombre}</b><span className="mt-1 flex items-center gap-1 text-sm text-slate-600"><MapPin size={14} />{item.obra?.nombre || item.organizacion.nombre}</span></span></button>)}</div></section></main>;
}
