import { Database, LockKeyhole, LogOut, ShieldCheck } from "lucide-react";
import { Button } from "@/shared/ui";
import { useAuth } from "@/features/auth/context/AuthContext";

export default function SuspendedServicePage({ membership }) {
  const { logout } = useAuth();
  return <main className="flex min-h-screen items-center justify-center overflow-hidden bg-gradient-to-br from-slate-950 via-emerald-950 to-slate-900 p-5 text-white"><div className="absolute h-96 w-96 rounded-full bg-emerald-400/10 blur-3xl" /><section className="relative w-full max-w-4xl rounded-[32px] border border-white/10 bg-white/[0.06] p-7 shadow-2xl backdrop-blur-xl sm:p-10"><div className="flex items-center gap-3 text-emerald-300"><ShieldCheck size={30} /><b className="text-xl">Carbono Zero</b></div><span className="mt-8 inline-flex rounded-full border border-amber-300/25 bg-amber-300/10 px-3 py-2 text-xs font-black uppercase tracking-[0.14em] text-amber-200">Acceso temporalmente suspendido</span><h1 className="mt-5 text-4xl font-black sm:text-5xl">Servicio suspendido</h1><p className="mt-4 max-w-3xl text-base leading-7 text-slate-200">El acceso operativo de esta organización se encuentra temporalmente suspendido. Toda la información ambiental permanece protegida y no ha sido eliminada.</p><div className="mt-8 grid gap-4 md:grid-cols-2"><article className="rounded-[22px] border border-white/10 bg-white/[0.06] p-5"><Database className="text-emerald-300" /><h2 className="mt-4 text-lg font-black">Datos protegidos</h2><p className="mt-2 text-sm leading-6 text-slate-300">Obras, registros, evidencias, indicadores y documentos permanecen almacenados de forma segura.</p></article><article className="rounded-[22px] border border-white/10 bg-white/[0.06] p-5"><LockKeyhole className="text-cyan-300" /><h2 className="mt-4 text-lg font-black">Reactivación</h2><p className="mt-2 text-sm leading-6 text-slate-300">Contacta al administrador de la plataforma para regularizar el servicio y recuperar el acceso.</p></article></div><footer className="mt-8 flex flex-col gap-4 border-t border-white/10 pt-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs uppercase tracking-widest text-slate-400">Organización actual</p><b>{membership?.organizacion_nombre}</b><p className="text-xs capitalize text-amber-200">Estado: {String(membership?.saas_estado || "suspendido").replaceAll("_", " ")}</p></div><Button variant="secondary" leftIcon={LogOut} onClick={logout}>Cerrar sesión</Button></footer></section></main>;
}
