export default function Tabs({tabs=[],activeTab,onChange}){return <div role="tablist" className="flex flex-wrap gap-1 border-b border-[var(--border-default)]">{tabs.map(tab=><button role="tab" aria-selected={activeTab===tab.value} key={tab.value} type="button" onClick={()=>onChange(tab.value)} className={`border-b-2 px-4 py-2 text-sm font-bold focus-visible:outline-none focus-visible:shadow-[var(--focus-ring)] ${activeTab===tab.value?"border-[var(--brand-primary)] text-[var(--brand-primary)]":"border-transparent text-[var(--text-muted)] hover:text-[var(--text-primary)]"}`}>{tab.label}</button>)}</div>}
