import { Button } from "./Button";
export default function Pagination({page,currentPage,totalPages,onChange,onPageChange,totalItems,pageSize,itemLabel="registros"}){const current=page||currentPage||1;const total=totalPages||Math.max(1,Math.ceil((totalItems||0)/(pageSize||1)));const change=onChange||onPageChange;if(total<=1)return null;return <nav aria-label={`Paginación de ${itemLabel}`} className="mt-4 flex items-center justify-between gap-3"><Button variant="secondary" size="sm" disabled={current<=1} onClick={()=>change(current-1)}>Anterior</Button><span className="text-xs text-[var(--text-muted)]">Página {current} de {total}</span><Button variant="secondary" size="sm" disabled={current>=total} onClick={()=>change(current+1)}>Siguiente</Button></nav>}
