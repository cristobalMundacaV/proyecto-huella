import { EmptyState,LoadingState } from "./Feedback";
export function TableShell({children,className=""}){return <div className={`overflow-auto rounded-[var(--radius-lg)] border border-[var(--border-default)] bg-[var(--bg-surface)] ${className}`}><table className="w-full min-w-[720px] border-collapse text-left text-sm [&_tbody_tr]:transition-colors [&_tbody_tr:hover]:bg-[var(--bg-surface-subtle)]">{children}</table></div>}
export function TableHead({children,className=""}){return <thead className={`bg-[var(--bg-surface-subtle)] text-center text-xs uppercase tracking-wide text-[var(--text-muted)] ${className}`}>{children}</thead>}
export function TableBody({children,loading=false,empty=false,columns=1}){if(loading)return <tbody><tr><td colSpan={columns}><LoadingState/></td></tr></tbody>;if(empty)return <tbody><tr><td colSpan={columns}><EmptyState title="Sin registros" description="No hay datos para los filtros seleccionados."/></td></tr></tbody>;return <tbody className="divide-y divide-[var(--border-subtle)]">{children}</tbody>}
export function TableCell({children,numeric=false,align,as:Tag="td",className=""}){const alignment=align==="left"?"text-left":align==="right"?"text-right":numeric?"text-center tabular-nums":"text-center";return <Tag className={`align-middle px-4 py-3 ${alignment} ${className}`}>{children}</Tag>}
